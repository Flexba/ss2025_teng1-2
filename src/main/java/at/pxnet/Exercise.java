package at.pxnet;

public class Exercise {
    /**
     * Calculate the sum of the parameter values a and b and return it
     */
    public static int add(int a, int b) {
        return 0;
    }


    /**
     * Convert the provided temperature from Celsius to Fahrenheit and print it.
     * The output must have the format. "The temperature is 123.4 F"
     * The output must always contain exactly one decimal place
     */
    public static void printTemperatureFahrenheit(float celsius) {
        System.out.println("The temperature is 0.0 F");
    }

    /**
     * Find the largest number in the array and return it
     */
    public static int maxNumber(int[] numbers) {
        return 0;
    }

    /**
     * Perform the calculation based on the operator on the parameters a and b.
     * When a division by 0 is to be calculated, return -1
     */
    public static int calculate(int a, int b, char operator) {
        return 0;
    }

    /**
     * Return true if the number is a prime, false otherwise
     */
    public static boolean isPrime(int number) {
        return false;
    }
}
